{
  "name": "astro-static-headers",
  "description": "Astro integration that captures headers and redirects during prerender and writes platform-specific config (Netlify, Cloudflare, Vercel).",
  "version": "1.0.5",
  "type": "module",
  "author": "Adam Bouqdib <adam@abemedia.co.uk>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/abemedia/astro-static-headers.git"
  },
  "bugs": "https://github.com/abemedia/astro-static-headers/issues",
  "homepage": "https://github.com/abemedia/astro-static-headers#readme",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "husky",
    "build": "tsdown src/index.ts src/middleware.ts --no-fixed-extension",
    "prepack": "pnpm build",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "biome format --write . && prettier --write --cache \"**/*.{md,yml,yaml,astro}\"",
    "lint": "biome lint --write .",
    "check": "biome check . && tsc --noEmit"
  },
  "keywords": [
    "astro-integration",
    "astro",
    "withastro",
    "headers",
    "redirects",
    "cloudflare",
    "netlify",
    "vercel"
  ],
  "engines": {
    "node": "^20.3.0 || >=22.0.0"
  },
  "packageManager": "pnpm@11.25.0",
  "peerDependencies": {
    "astro": ">=5.0.0"
  },
  "dependencies": {
    "@astrojs/underscore-redirects": "^1.0.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@types/node": "^20.19.43",
    "astro": "^7.2.10",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "lint-staged": {
    "*": "biome check --write --files-ignore-unknown=true --no-errors-on-unmatched",
    "*.{md,yml,yaml,astro}": "prettier --write"
  },
  "publishConfig": {
    "provenance": true
  }
}
